syntax = "proto3";

package customer.events;
option java_multiple_files = true;

message CustomerRegisterEvent {
  string customerId = 1;
  string registerDate = 2;
}

message CustomerDeleteEvent {
  string customerId = 1;
}